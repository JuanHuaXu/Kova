{
  "id": "channel-capability-preflight",
  "surface": "channel-capability-preflight",
  "title": "Channel Capability Preflight",
  "objective": "Prove the target OpenClaw runtime exposes the shared message-channel capability contract expected by Kova before adapter-specific conformance runs.",
  "tags": [
    "channel",
    "capability",
    "preflight",
    "contracts"
  ],
  "targetKinds": [
    "npm",
    "release",
    "runtime",
    "local-build"
  ],
  "timeoutMs": 180000,
  "auth": {
    "mode": "skip"
  },
  "thresholds": {},
  "phases": [
    {
      "id": "provision",
      "title": "Provision Runtime Env",
      "intent": "Create a disposable OpenClaw environment with the selected target runtime so Kova can probe the release-shaped package artifact.",
      "commands": [
        "ocm start {env} {startSelector} --json"
      ],
      "evidence": [
        "OCM start JSON",
        "env name",
        "runtime binding"
      ],
      "healthScope": "readiness",
      "collectionIntent": "service-only"
    },
    {
      "id": "channel-capability-preflight",
      "title": "OpenClaw Channel Capability Preflight",
      "intent": "Import the selected OpenClaw runtime's packaged channel-message contract helpers and emit one preflight proof row per OpenClaw channel capability catalog entry.",
      "commands": [
        "node {kovaRoot}/support/run-channel-capability-preflight.mjs --env {env} --artifact-dir {artifactDir} --timeout-ms 120000"
      ],
      "evidence": [
        "OpenClaw runtime channel-message package export",
        "OpenClaw runtime channel contract helper results",
        "preflight capability proof rows"
      ],
      "healthScope": "none",
      "collectionIntent": "skip-env"
    }
  ],
  "proves": [
    "preflight"
  ]
}
